[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["cd ../.. && corepack enable && pnpm install --frozen-lockfile=false"]

[phases.build]
cmds = ["cd ../.. && pnpm build"]

[start]
cmd = "node dist/index.js"
